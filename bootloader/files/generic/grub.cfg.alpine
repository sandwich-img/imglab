set timeout=2
insmod all_video
menuentry "Linux" {
    search --no-floppy --fs-uuid --set=root BOOT_UUID
    echo	'Loading Linux ...'
    linux   /vmlinuz-vanilla root=LABEL=rootfs rw rootfstype=ext4
    echo	'Loading initial ramdisk ...'
    initrd  /initramfs-vanilla
}
